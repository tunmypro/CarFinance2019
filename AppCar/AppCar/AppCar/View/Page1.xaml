<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppCarFinance.Page1"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModel1="clr-namespace:AppCarFinance.ViewModel;assembly=AppCar"
    Title="รายชื่อ">

    <ContentPage.BindingContext>
        <viewModel1:MemberViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <ListView HasUnevenRows="True" ItemsSource="{Binding listMemberses}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Label FontSize="Medium" Text="ชื่อ" />
                                <Label FontSize="Medium" Text="{Binding memname}" />
                                <Label FontSize="Medium" Text="{Binding memlname}" />
                                <Label FontSize="Medium" Text="อายุ" />
                                <Label FontSize="Medium" Text="{Binding memage}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>